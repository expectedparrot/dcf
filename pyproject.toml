[project]
name = "dcf"
version = "0.1.0"
description = "Append-only DCF CLI"
requires-python = ">=3.11"
dependencies = ["openpyxl>=3.1"]

[project.scripts]
dcf = "dcf.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
